## Packages
framer-motion | For smooth page transitions and entry animations
recharts | For visualizing the credibility score and metrics
clsx | For conditional class names
tailwind-merge | For merging tailwind classes utility

## Notes
- Tailwind config needs to extend fontFamily for 'display' and 'body'
- Backend API at /api/analyze expects { url?: string, text?: string }
